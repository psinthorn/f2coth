# Tracker — PDPA Privacy Centre (RUN-2026-06-12-2)

_Generated by Tracker agent · 2026-06-12_

---

## Milestone

| Field | Value |
|---|---|
| **Name** | PDPA Privacy Centre v1 |
| **Target** | 2026-06-19 |
| **Goal** | Ship a fully PDPA-compliant privacy layer: cookie consent banner, DSR submission form, enriched privacy policy, full terms of service, and admin DSR queue — bilingual EN + TH. |

---

## Issues shipped (this pipeline run)

| # | Title | Type | Area | Effort | Acceptance criteria | Linked |
|---|---|---|---|---|---|---|
| 1 | `db: add cookie_consents + data_subject_requests tables` | feat | dba | S | Migration 015 applies cleanly; UNIQUE index on visitor_id; partial index on DSR due_date; retention TTL columns on leads + chat_sessions | 015_pdpa_privacy_centre.sql |
| 2 | `backend: DSR CRUD endpoints on auth-api` | feat | backend | M | POST /api/privacy/dsr returns 201; GET + PATCH require admin JWT; invalid request_type → 400; invalid email → 400; invalid UUID → 400 | privacy.go |
| 3 | `backend: cookie consent recording on lead-api` | feat | backend | S | POST /api/consent upserts; visitor_id UUID-validated; withdraw endpoint sets withdrawn_at; $5::inet param correct | consent.go |
| 4 | `backend: PDPA guardrails in chatbot system prompt` | feat | backend | S | Chatbot refuses to confirm other visitors' data; refers to /privacy; discloses 90-day retention | chat.go |
| 5 | `frontend: CookieBanner component` | feat | frontend | M | Shows on first visit; "Essential only" / "Accept all" / granular manage; POSTs to /api/consent; localStorage persists choice; locale prop wired; ARIA dialog label | CookieBanner.tsx |
| 6 | `frontend: DSRForm component` | feat | frontend | M | 6 request types; required fields enforced; success/error states; POSTs to /api/privacy/dsr; ARIA labels; Thai characters round-trip | DSRForm.tsx |
| 7 | `frontend: extend /privacy page with full PDPA content` | feat | frontend | S | 12 sections incl. controller, basis, retention, cross-border, breach notification, DSR form; `id="dsr"` anchor; metadata.privacy.title set | privacy/page.tsx |
| 8 | `frontend: extend /terms page with Thai-law-compliant clauses` | feat | frontend | S | 9 sections incl. governing law (Bangkok courts), limitation of liability, auto-renewal clarity | terms/page.tsx |
| 9 | `i18n: 814 bilingual keys (EN + TH) for privacy centre` | feat | frontend | S | `make i18n-check` passes; 814 keys parity; Thai PDPA legal terminology correct | en.json, th.json |
| 10 | `devops: Traefik routes for /api/privacy + /api/consent` | chore | devops | S | `PathPrefix(/api/auth) \|\| PathPrefix(/api/privacy)` on auth-api; `PathPrefix(/api/leads) \|\| PathPrefix(/api/consent)` on lead-api | docker-compose.yml |

---

## Open follow-up issues (not in this run — backlog)

| # | Title | Type | Area | Effort | Why deferred |
|---|---|---|---|---|---|
| F-1 | `backend: enqueue dsr_received_requester notification email` | feat | backend | M | TODO comment in privacy.go; notification-api exists but DSR templates need wiring |
| F-2 | `backend: enqueue dsr_received_staff notification email` | feat | backend | S | Same as above — staff alert |
| F-3 | `backend: scheduled job — anonymise expired leads + chat sessions` | feat | backend | L | Cron/worker needed; no job runner in current stack |
| F-4 | `admin: DSR queue UI in admin portal` | feat | frontend | L | Admin needs UI to see/assign/respond to DSRs; API is ready |
| F-5 | `legal: Data Processor Agreement (DPA) template for hotel clients` | chore | docs | M | Required by PDPA Section 40 when processing guest PII on behalf of SALA, Miskawaan, Putahracsa |
| F-6 | `security: add IP rate limit to /api/privacy/dsr (public endpoint)` | fix | devops | S | Currently inherits auth-api rate limit (10/min avg); DSR should have its own tighter limit |
| F-7 | `security: add IP rate limit to /api/consent (public endpoint)` | fix | devops | S | Currently inherits lead-api rate limit; consent should be separated |
| F-8 | `backend: cookie consent lookup on page load` | feat | backend | S | GET /api/consent/{visitorId} is defined but not called from frontend on load |
| F-9 | `legal: appoint a Data Protection Officer (DPO)` | chore | docs | S | PDPA Section 41: required if processing on a large scale or sensitive data; legal review needed |

---

## Dependencies

```
015 migration must be applied before auth-api + lead-api can start.
Traefik route update (docker-compose.yml) requires `make prod-up` restart.
F-1 + F-2 (notifications) require notification-api DSR template wiring.
F-3 (anonymisation job) requires a job runner (pg_cron or external scheduler) — spike first.
F-4 (admin DSR UI) requires F-1+F-2 to be done for full workflow.
```

---

## Risks

- **Thai PDPA enforcement** — PDPC has issued fines since 2022; F-9 (DPO appointment) should be reviewed with a Thai attorney within 30 days.
- **Anthropic cross-border transfer** — disclosed in privacy policy; F2 should obtain Anthropic's DPA terms in writing before GDPR-scope hotel clients go live.
- **Anonymisation gap** — F-3 is unbuilt; until it ships, expired data is not automatically anonymised. Manual process needed.
- **Rate limit gap** — F-6 + F-7 are unbuilt; `/api/privacy/dsr` is a public POST endpoint that could be abused for spam DSRs.
