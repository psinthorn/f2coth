# F2 MCP servers — shared env template.
# Copy per-server or set in Claude Code MCP config (see README.md).

# Postgres connection. Points at the docker-compose stack by default.
DATABASE_URL=postgres://f2:f2@localhost:5432/f2_website
